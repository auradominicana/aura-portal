{
  "name": "aura-portal",
  "version": "1.0.0",
  "description": "Portal Aura — punto de entrada único a Ventas, Cobros y Operaciones (SSO)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=20.16.0"
  }
}
